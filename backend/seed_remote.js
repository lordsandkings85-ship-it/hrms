const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function seed() { const company = await prisma.company.create({ data: { name: 'Acme Corp' } }); const role = await prisma.role.create({ data: { companyId: company.id, name: 'Super Admin', isSystem: true, permissions: { create: [{ module: 'ALL', action: 'ALL' }] } } }); const pass = await bcrypt.hash('admin123', 10); await prisma.user.create({ data: { companyId: company.id, email: 'admin@acme.com', passwordHash: pass, roleId: role.id, isSuperAdmin: true } }); console.log('Seeded admin@acme.com (admin123)'); } seed().catch(console.error).finally(() => prisma.$disconnect());
