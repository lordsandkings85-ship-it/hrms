# Workora HRMS — Audit Ledger (Phase 0 inventory)

Generated by 4 parallel audits. Status legend: ✅ fixed · ⛔ open.

## 1. Frontend — pages & navigation
- 123 pages, all reachable; 0 placeholder pages; 0 orphan files.
- ✅ `CommandPalette.tsx` `/attendance` → `/attendance/daily`; `AuxiliaryRoutes.ts` deleted; `API_BASE_URL` removed from constants.
- `HrGuard` = alias of `AdminGuard`; guards unused — inline `canAccessPath`/`isAdminOnlyPath` in App.tsx.
- Employee-side label drift: `/leave/requests` renders MyLeavePage etc. (functional).

## 2. Frontend — dead/fake buttons
- ✅ Shared: DataTable real CSV export (`exportable`/`exportFilename`); Layout Go (navigates to first matching nav item); Layout Bell (real notifications from `dashboardApi.summary()`, click navigates by id prefix).
- ✅ AdvancedEmployeeForm Manage (probation start/end) + Reset (restores initialData/empty); ApprovalQueue Filter.
- ✅ Template A (10 files): Assets, SendCredentials, BulkCompliance, Loans, Payslip, Salary, Transfer, FinalSettlement, ImportManagers, Resignation — all wired to real APIs.
- ✅ Template B (3 files): MarkAttendanceApproval (manual punch modal via `attendanceApi.manualPunch` + Filter), GeoAttendanceApproval (View Location map + zone Filter), CorrectionRequestApproval (real regularization pipeline approve/reject + Filter).
- ✅ `RegularizationApprovalPage` Filter; `TeamDailyAttendanceReportPage` Filter; `TaxDeclarationsPage` Browse Files (real multi-file upload via `documentsApi.upload`, proof list + remove); `MyDocumentsPage` search input + category Filter select; `DailyReportPage` Filter/This Month; `BillingPage` PDF (real jsPDF receipt).
- ✅ **Fake exports**: `EmployeesPage` W→real .doc, X→real .xls, PDF→real jsPDF (no hardcoded fallbacks); `Form16Page` → real Form 16 PDF (`utils/form16PDF.ts`, "Digitally Signed" pill → honest "Generated from payroll records"); `SalaryRevisionHistoryPage` Letter → real .doc increment letter.

## 3. Frontend ↔ backend API wiring
- Every client call resolves to a backend handler — no 404 risk, no method mismatch.
- ✅ **P2 page smoke (live, 2026-08-15)**: 195 curl GET checks (HR/Employee/SuperAdmin tokens × 65 endpoints incl. all dynamic `:id` routes with real ids, setup kinds, reports w/ query strings) → 0 failures (all 200, or expected 403 for role-guarded endpoints; no 500s). Detail routes `/leave/holidays/:id`, `/leave/types/:id`, `/shifts/:id`, `/shifts/assignments/:id` are DELETE-only client routes (no GET handlers — never called by any page, verified via caller grep). `/training/enrollments` GET doesn't exist and has no frontend caller. Frontend tsc compiles all 123 pages.
- ✅ **P8 cross-module journeys (live, 2026-08-15)**: (1) Exit→FnF journey on disposable employee: initiate (10-item checklist auto-created, employee status→archived) → complete all checklist items → exit interview note auto-advances status `initiated→fnf` → advanceStatus `approved`; FnF initiate computes gratuity (15×basic×floor(years)/26 — 6 yrs → 86538.46), notice-period recovery (74 days × basic/26), net settlement; `PATCH /fnf/:id/overrides` recomputes net (15384.62−2500=12884.61); approve passes (no pending assets); both lists scoped (employee → 403 on exit/fnf list). (2) Payroll journey: cycles list → Aug-2026 cycle payslips match P4 hand-verification (HR-001 net 43927.86, LKE1807 net 4364.29); re-run payroll → 201 `payslipCount: 0` (idempotent, no dupes); send-payslips → 400 (SMTP unconfigured, env-gated). (3) Leave approval: employee apply → HR approve → balance used 6.5→8.5 (+2 days). Test data cleaned (employee/exit/fnf/leave rows removed, balance restored).
- ✅ **leave.apply validation fix**: missing/blank `leaveTypeId`/`startDate`/`endDate` previously → Prisma validation 500; now `BadRequestException` 400 (leave.service.ts). Tests still 52/52.

## 3b. DB — P9 index coverage & migration-drift findings
- ✅ **Hot-path composite indexes** (migration `20260815000400_hot_path_indexes`, applied via `migrate deploy`): `Holiday(companyId,date)`, `LeaveRequest(employeeId,status)`, `LeaveRequest(status)`, `Payslip(employeeId,payrollCycleId)` — all verified present in information_schema. `AttendanceLog(employeeId,date)` already existed. 52/52 tests still green.
- ⛔ **Pre-existing FK drift (found 2026-08-15)**: live DB has 62 real FK constraints vs 97 `*_fkey` indexes → ~35 tables (e.g. AdditionalPayout) have relation indexes but NO actual foreign keys (Hostinger dump imported without them; sync_schema migration only partially reconciled). Consequence: no FK enforcement on those tables; and `prisma migrate dev` shadow-DB replay fails with MySQL 1553 (sync_schema DROP INDEX conflicts with baseline-created FK). **Do NOT run `migrate dev`**. Working replay path (P16): import dump → `migrate resolve --applied 20260815000000_baseline` → `migrate deploy` (this is exactly how the local DB was rebaselined; `_prisma_migrations` shows sync_schema rolled-back+re-applied once). Full FK reconciliation on live DB = separate, destructive-adjacent project; defer to handoff with this doc.
- ✅ `documentsApi.upload` now wired (TaxDeclarationsPage proofs).
- ✅ **P14 dead client method wiring**: recruitment offer flow (createOffer w/ CTC modal → moveStage offered; acceptOffer → auto-onboard joined; offer pending/accepted pill; backend now rejects missing candidate email; `Offer.createdAt` added via migration `20260815000300_offer_created_at` for latest-offer lookup); leave bulk Approve/Reject (DataTable bulkActions) + analytics strip on LeaveAdminPage; admin geofence config on AttendancePolicyPage (`setGeofence`); payroll tax-preview modal + per-cycle payslips viewer (ProcessSections); employee "+ Add KPI" goal creation (MyResponsibilitiesPage); ApiTableEditor edit support (Edit3 → row → Save) wired to all 21 setup editors (performance/compliance/tax/org-masters PUTs).
- Removed dead client methods (zero callers): leaveApi.listPending, payrollApi.runPayroll (old), shiftsApi.addHoliday, integrationApi google config, configApi.remove. Kept shiftsApi.listHolidays (callers: HrDashboard, MyShiftsPage).
- `frontend/.env` → Render URL; no vite proxy; backend CORS allow-list only 2 vercel + localhost:5173.

## 4. Backend — mock/hardcoded behaviors
- `timesheets.service.ts:23` $100/hr billable; `recruitment.service.ts:72` auto 10L CTC offer; `recruitment.service.ts:127` + `employees.service.ts:44` `password123`; `fnf.service.ts:38` zeros; `billing.service.ts:34-44` mock pricing + 18% GST; `documents.service.ts:21` simulated e-sign; `dashboard.service.ts:218` estimated cost; `main.ts:47` hardcoded URL.

## 5. Backend — company scoping gaps (~25)
- ✅ **Resolved 2026-08-15 (P10 batch)**: leave — `listForEmployee`/`balances` now verify employee belongs to company (404 otherwise), `cancel` verifies company + ownership; attendance — `checkOut` now verifies log belongs to company (Forbidden); shifts — `assign`/`requestChange`/`generateDepartmentRoster` verify employee+shift belong to company (404); training — `TrainingCourse`/`CourseEnrollment` gained `companyId` + relation (migration `20260815000500_training_company_scope`, backfilled to seed company, FK+index added), all training reads/writes (listCourses/create/enroll/updateProgress/autoAssign) company-scoped, controller passes companyId; recruitment (add/moveStage/scheduleInterview/createOffer/onboard/evaluate/remove/feedback) + payroll (addPayout/deletePayout/getCyclePayslips) + integrations (connect/disconnect) verified already company-scoped. All live-verified + 52/52 tests.
- ✅ Remaining: `billing.generateMonthlyInvoices` intentionally global (SuperAdmin-only cron) — only gap left, by design.
- ✅ employee-services self-scoping: approved in P12 — all approve/reject routes verified `@Permissions(employee-services:approve)` + service company-scoped; approve uses request `:id` (never employeeId) so self-bypass cannot fire (see §8).

## 6. Backend — DTO validation
- Global `ValidationPipe` (whitelist, transform) present but was inert on `body: any` handlers. **2026-08-15: money-critical paths now validated** via class-validator DTOs (all live-verified 400 on bad payloads, 201 on valid, 52/52 tests):
  - `payroll.dto.ts`: SalaryStructureDto (all components ≥0, effectiveFrom ISO8601 w/ empty-string tolerance for frontend default), RunPayrollDto (`month` 1-12, `year` ≥2000 — catches month 13 which previously persisted a cycle), AddPayoutDto.
  - `fnf.dto.ts`: InitiateFnfDto, FnfOverridesDto (money fields ≥0).
  - `employee-services.dto.ts`: CompOff/FlexibleHoliday/Overtime/OptionalHoliday/Loan/SalaryRevision/TaxDeclaration/ApproveTaxDeclaration (negative loan amounts, hours, CTc now rejected).
  - `expenses.dto.ts` / `travel.dto.ts` / `timesheets.dto.ts`: SubmitExpense/RequestTravel/SubmitTimesheet (negative amounts, hours ≤24).
- ✅ `leave.apply` 400s on missing leaveTypeId/startDate/endDate (BadRequestException, P8).
- ⛔ Remaining breadth: ~70 lower-risk handlers still inline-typed (no runtime validation) — e.g. setup CRUDs, org-masters, announcements. Mitigated by PermissionsGuard + CompanyScopeGuard on those routes; candidate for a later hardening pass, not blocking.
- Test probes created during validation (payroll cycles 12/2025 + 13/2026, salary structure, payout, loan) cleaned from DB.

## 7. Backend — non-transactional multi-writes
- ✅ **All candidate paths verified transactional (2026-08-15) — audit list was stale.** Code-read each: `runPayroll` (payslip loop in `$transaction`), `generateMonthlyInvoices` (per-company loop in `$transaction`), `employees.create` (employee+user+nested records), `auth.register` (company+role+employee+user), `exit.initiate` (exit + checklists), `leave.approve` (balance increment + status), `leave.approveCancellation` (balance restore + status), leave-accrual cron (upsert loop), `training.autoAssign`, `recruitment.removeCandidate` (interviews+offers+candidate), attendance regularization approve/reject (request + attendanceLog). No untransactional multi-write remains.

## 8. Backend — guard gaps (critical)
- ✅ **All guard gaps resolved (2026-08-15)** — see §5, §6, §8 above for full list. Only intentionally-open self-service reads remain (`leave/types`, `attendance/geofence`, `training/courses`, `dashboard/summary` redacted for employees) plus SA-only `billing.generateMonthlyInvoices`.

## 9. Test coverage (2026-08-15)
- 109 tests across 8 spec files, all green.
- **`permissions.guard.spec.ts` (16 tests)**: added 7 tests for the `isSalaryMutation` exclusion (PATCH salary-structure/salary-revision blocked for employee self-bypass), `isEmployeeRecordMutation` exclusion (PATCH/DELETE on /employees/:id blocked for employee self-bypass), GET self-bypass still allowed, body.employeeId self-bypass on non-salary non-employee mutations still works.
- **`payroll.dto.spec.ts` (50 tests, new)**: validates all money-path DTOs — RunPayrollDto (month 1-12, year ≥2000), SalaryStructureDto (basic required, empty effectiveFrom tolerated), AddPayoutDto, FnfOverridesDto, LoanDto, SalaryRevisionDto, OvertimeDto, TaxDeclarationDto, SubmitExpenseDto, SubmitTimesheetDto (hours 0-24), RequestTravelDto.
- Existing: `permissions.guard.spec.ts` (9→16), `company-scope.guard.spec.ts` (9), `tax.calculator.spec.ts` (12), `payroll.service.spec.ts` (7), `crypto.util.spec.ts` (9), `mail.service.spec.ts` (4), `seeder.service.spec.ts` (3).
- ⛔ No integration/E2E tests for live API flows (API-level smoke via P2 curl tests covers this).

## 10. Data Persistence Audit (2026-08-16/17) — Waves 1-7
Full field-level audit mapping every frontend input → API → DTO → Prisma → DB column. Found 4 CRITICAL + 9 HIGH + 11 MEDIUM bugs.

### Wave 1: DTO Rewrite ✅
- Rewrote all 7 nested DTOs in `create-employee.dto.ts` to match Prisma schema + frontend form. ~40 fields previously silently stripped by `whitelist:true` now persist. DATE_FIELDS updated. Live-verified all fields.

### Wave 2: Leave-Payroll Integration ✅
- **C3**: `leave.approve()` now creates `AttendanceLog` records with `status: 'on_leave'` per approved day (excludes Sundays + company holidays).
- **C4**: Fixed `bal.balance` → `allotted - used` in 4 frontend files (MyLeavePage, EmployeeDashboard, HrDashboard, LeaveSections).
- **H6**: `setCompOffStatus()` async — on approval, creates/credits "Compensatory Off" LeaveBalance.
- **H9**: `approveCancellation()` holiday exclusion logic for balance restoration + deletes `on_leave` AttendanceLogs (expanded UTC date range).
- **H4**: `AdvancedEmployeeForm` status casing: `"active"`/`"inactive"` (lowercase) with display capitalization.
- **CompanyScopeGuard**: 7 missing tables added to IDOR resolver (`compOffRequest`, `flexibleHolidayRequest`, `overtimeRequest`, `optionalHolidayRequest`, `loanRequest`, `taxDeclaration`, `leaveCancellationRequest`). Updated guard spec.

### Wave 3: Salary/Payroll ✅
- **H1**: `runPayroll()` fetches `AdditionalPayout` records and adds sum to payslip gross (breakdown includes `additionalPayout`).
- **H2**: Reads `adminInfo.pfAsPerGovt`/`esicApplicable` — auto-computes PF/ESI when `true`, uses manual structure values when `false`.
- **H5**: `LifecycleWorkflows.tsx` promotion form includes DA, conveyance, medical fields (3→6 salary components).
- **M4**: `setSalaryStructure()` creates `SalaryRevision` record in transaction.

### Wave 4: Frontend Property Fixes ✅
- **H3**: `designation?.name` → `.title` in 6 occurrences (EmployeeTransferPage, ShiftAssignPage, EmployeeDirectoryPage).
- **H7**: Removed hardcoded `'NA'` fallback → `'—'` (em dash).
- **H8/M11**: Grade dropdown populated from HRMaster via `orgMastersApi.list('masters')` in both TransferForm and PromotionForm.
- **M10**: Removed `|| true` from flexible holiday filter.

### Wave 5: Permissions & Security ✅
- **M2**: Added `@Permissions({ module: 'leave', action: 'edit' })` to `POST :id/cancel` endpoint.
- M7/M8 (admin page relocation) deferred — cosmetic/routing change, no security impact.

### Wave 6: Cross-Portal Cache Invalidation ✅ (M1)
- **19 files** — every approval/approve/reject mutation now invalidates employee-side query keys:
  - Leave: `leave-history`, `leave-balances`, `leave-balances-dash`, `dashboard-summary`
  - CompOff: `comp-off-mine`
  - Flexible holiday: `flexible-holiday-mine`
  - Overtime: `employee-overtime-logs`
  - Travel: `my-travel-requests`
  - Expenses: `my-expenses-list`
  - Loans: `my-loans`, `company-loans`
  - Shifts: `shifts-list`, `my-profile`, `attendance-today`, `attendance-history`
  - Tax: `tax-declarations`
  - Performance: `my-goals`
  - FnF: `fnf-employee`
  - Timesheets: switched from `refetch()` to `invalidateQueries`
  - Regularization/correction: `attendance-history`, `attendance-today`, `attendance-summary`, `attendance-summary-dash`

### Wave 7: Remaining Fixes ✅
- **M3**: Regularization form now has time inputs for check-in/check-out (was using `new Date()` for both).
- **M5**: `ManualPunchPage` now invalidates `attendance-history`, `attendance-today`, `dashboard-summary`.
- **M6**: Feedback ratings persisted — added `ratings Json?` column to `HelpdeskTicket` (migration `20260816000000_helpdesk_ratings`), service, and frontend sends ratings on feedback type.
- **M9**: `LeaveApprovalPage` passes `{ status: 'pending' }` to API (was fetching all).

### Migrations ledger (applied)
`20260815000000_baseline`, `20260815000100_sync_schema`, `20260815000200_money_decimal`, `20260815000300_offer_created_at`, `20260815000400_hot_path_indexes`, `20260815000500_training_company_scope`, `20260816000000_helpdesk_ratings`.

### Final status
- 109/109 tests pass across 8 spec files.
- Backend + frontend build green.
- 17 endpoints smoke-tested live → 0 failures.
